document.addEventListener('DOMContentLoaded', () => {
  const searchForm = document.getElementById('search-panel__form');
  const queryInputElement = searchForm.querySelector('input#lib-search-box');
  const querySubmissionElement = searchForm.querySelector('input[name=query]');
  searchForm.addEventListener('submit', (e) => {
  })
});
